name:  Docker-Multi-Arch
  
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
     inputs:
      tagName:
        description: 'Tag Name'     
        required: true
        default: 'latest'
        
jobs:
  build:
    runs-on: ubuntu-latest
    name: build for amd64/arm64
    steps:
    - uses: actions/checkout@v2
      with:
          submodules: recursive

    - name: Login to GitHub Container Registry
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
        
    # Setup hardware emulator using QEMU
    - name: Set up QEMU
      uses: docker/setup-qemu-action@v3
        
    # Setup Docker Buildx for multi-arch images
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Build Check
      if: |
          github.repository != 'ilmax/loxilb-arm-fix'
      uses: docker/build-push-action@v4
      with:
        context: .
        platforms: linux/arm64
        push: false
        tags: ghcr.io/ilmax/loxilb:latest
          
    - name: Build and push to latest
      if: |
          github.repository == 'ilmax/loxilb-arm-fix' 
          &&  github.event.inputs.tagName == ''          
      uses: docker/build-push-action@v4
      with:
        context: .
        platforms: linux/arm64
        push: true
        tags: ghcr.io/ilmax/loxilb:latest

    - name: Build and push to given tag
      if: |
          github.repository == 'ilmax/loxilb-arm-fix' 
          &&  github.event.inputs.tagName != ''          
      uses: docker/build-push-action@v4
      with:
        context: .
        platforms: linux/arm64
        push: true
        build-args: TAG=${{ github.event.inputs.tagName }}
        tags: ghcr.io/ilmax/loxilb:${{ github.event.inputs.tagName }}
    